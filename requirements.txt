# FlexPosit — exact versions behind the MICRO 2026 results.
# `pip install flexposit` (pyproject.toml) only sets minimum versions.
# install.sh installs torch first from the CUDA 11.8 (or CPU) index.

torch==2.4.1

# Transformer models
transformers==4.45.2
tokenizers==0.20.1
accelerate==1.10.1
safetensors==0.4.5
sentencepiece==0.2.0
huggingface-hub==0.26.1

# Datasets (WikiText-2)
datasets==3.0.2

# Scientific stack
numpy<2.0
scipy==1.15.3

# Utilities
tqdm
matplotlib
pandas

# Testing
pytest
